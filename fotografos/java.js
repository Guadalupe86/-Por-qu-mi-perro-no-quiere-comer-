function navigate(index) {
    console.log("Navigating to section:", index);
    // Aquí puedes agregar lógica para cambiar de sección, por ejemplo, mostrar/ocultar contenido según el índice
    // o utilizar un sistema de enrutamiento si el sitio tiene varias páginas.
}
